version: '1.0'

steps:
  build-all:
    type: composition
    description: build it all 
    composition:
      version: '2'
      services:
        mongo:
          image: mongo
    composition-candidates:
      b7beet:
        image: 'olsynt/b7beet:master'
        ports:
          - 3000
        environment:
          MONGO_URI_DEV: 'mongodb://mongo/b7beet'
    
  
  
