'use strict';

import route from './route.component';

describe('Component: routeComponent', function() {
  beforeEach(angular.mock.module(route));
  beforeEach(angular.mock.module('stateMock'));
  beforeEach(angular.mock.module('socketMock'));
});
